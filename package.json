{
  "name": "wuerfelduell",
  "private": true,
  "version": "28.7.0",
  "type": "module",
  "scripts": {
    "build": "npm run build:styles",
    "build:styles": "node scripts/build-styles.mjs",
    "check": "node scripts/build-styles.mjs --check && node scripts/verify-build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
